"""GraphQL query/mutation strings grouped by resource, reusable via fragments.

The bottom layer of the ``shopify`` package: pure strings, no imports from
``shopify.operations`` or ``tools``. See ``shopify`` package docstring for the
layering rule.
"""
